---
name: Hardware report
about: Add your host / dock / GPU to HARDWARE.md, working or not
title: "hardware: <host> + <dock>"
labels: hardware
---

Run `xgm-egpu report` (while the eGPU is active if it got that far) and paste
everything it prints below. Then say which stage of `go` you reached, or what
happened instead.

```
(xgm-egpu report output)
```

```
(xgm-egpu detect output)
```

Dock: official (which model) or DIY (osy variant and revision, connectors)?
Anything you had to do differently from the README?
